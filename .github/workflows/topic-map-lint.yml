name: topic-map-lint

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  topic-map-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Topic map drift check (warning-only)
        run: python3 tools/topic_map/topic_map_lint.py --check
