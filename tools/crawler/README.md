# Crawler

## Without LLM

```bash
python3 crawl_campus.py --output ./data --no-llm
```

## Add URLs

```bash
python3 crawl_campus.py --urls extra_urls.txt --output ./data
```

## Other Arguments

```bash
--delay 2.0
--retries 5
--timeout 10
```
